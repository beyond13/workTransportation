# 工作记录与技术笔记总结

> 覆盖范围：仓库当前全部文本资料、Custom Report Type 配置截图与 Gemini 使用界面截图。
> 目的：把零散的入职复盘、聊天记录、SOP 和 Salesforce 元数据整理成可执行的知识地图。
> 说明：本文区分“已形成的做法”和“仍待项目环境验证的假设”；后者不能直接当作生产结论。

## 一句话概览

这批记录围绕一个实际项目主线展开：将大量 FAQ / Salesforce Knowledge 迁移到 Experience Cloud，并让访客能够按分类查看内容；同时补齐报表、富文本图片、前端嵌入、安全权限与 Figma 还原能力。

```text
Excel / 旧系统 FAQ
  → 字段与 Category 映射
  → Knowledge + 富文本图片迁移
  → Data Category 关联
  → Experience Cloud 展示
  → Guest User / SEO / iframe 验证
  → 管理端 List View、Custom Report Type 与日常维护
```

## 已沉淀的核心能力

### 1. Salesforce Knowledge 与 FAQ 迁移

- 区分 `Knowledge__ka`（文章本体）和 `Knowledge__kav`（文章版本）；业务字段、语言、发布状态通常围绕版本对象维护。
- 用“先 10～20 条、覆盖不同分类树、再扩大”的方式验证迁移 Pipeline，而不是直接导入约 4000 条 FAQ。
- 迁移输入需要显式维护 `FieldMapping` 与 `CategoryMapping`；AI 可做重复转换，但不能猜 Category API Name。
- `ArticleBody` 含 HTML、逗号、引号和换行时，CSV 必须按 RFC 4180 进行 UTF-8 转义，并在 Data Loader Preview 中确认列数和内容。

对应资料：[runBook.md](./runBook.md)、[0722](./0722)、[listview確認](./listview確認)。

### 2. 分类、报表与后台可维护性

- Data Category Group 的文件名前缀是 Group API Name；节点的 `<name>` 是分类 API Name，叶节点通常用于文章实际关联。
- `Knowledge__DataCategorySelection` 的关键映射是：`ParentId = Knowledge__kav.Id`、`DataCategoryGroupName = Group API Name`、`DataCategoryName = Node API Name`。
- 已有的 POC Report Type 以 `Knowledge__ka` 为主对象，通过外连接关联 `Knowledge__kav`，并将标题、发布状态、语言和 FAQ 自定义字段暴露到 Report 中。
- 对 4000 条 FAQ 而言，后台的 List View 至少要能按 Title、Status、Language、Category 和 Last Modified 搜索、筛选、排序；这比单篇页面的美观更直接影响日常运用。

对应资料：[Data Category XML 说明](<./Salesforce Data Category Group XML の全体構造まとめ.md>)、[Report Type XML](./FAQ_Knowledge_with_Categories.reportType-meta.xml)、[Custom Report Type 截图](./カスタムレポートタイプ.png)。

### 3. Experience Cloud、Guest User 与 iframe

- 当前设想是：官网通过 iframe 提供“不跳转”的体验，Experience Cloud 承载可公开访问的 Knowledge 页面，文章独立 URL 用于搜索引擎收录。
- 能否嵌入取决于两端：Experience Cloud 侧的 Clickjack Protection / `frame-ancestors`，以及官网侧 CSP 的 `frame-src`；HTTPS、`sandbox` 属性和第三方 Cookie 也会影响实际行为。
- Guest User 权限是最高优先级风险：对象权限、字段级安全（FLS）和记录共享（OWD / Sharing Set / Sharing Rule）必须分别验证；管理员视图不能替代匿名窗口或真实外部用户测试。
- 生产环境应使用“指定域名白名单”嵌入。早期的“允许所有页面嵌入”仅适合隔离测试，不能带入生产环境。

对应资料：[iframe嵌套ExperienceCloud.md](./iframe嵌套ExperienceCloud.md)、[Experience Cloud 速查清单](./Experience_Cloud_速查清单.md)、[入职第一天复盘](./2026-07-13_入职第一天复盘.md)。

### 4. 富文本图片迁移

推荐的可追溯链路为：

```text
旧图片 ID 命名文件
  → ContentVersion 上传（得到 068… ID）
  → ContentDistribution 创建公开链接
  → 导出 ID 与 ContentDownloadUrl 字典
  → 脚本按旧 ID 替换 FAQ HTML
  → Data Loader 回填 Knowledge 富文本字段
```

这条流程的关键不是“上传图片”本身，而是保留旧 ID 到新 URL 的稳定映射表，以便批量替换、重跑和审计。

对应资料：[富文本图片迁移 SOP](<./Salesforce 富文本图片迁移标准作业流程>)。

### 5. Figma → CSS → LWC 的实现与验证

- Figma Dev Mode 主要用于读取字号、字重、行高、颜色、W/H、Padding、Gap 与组件结构；“Ready for dev”是设计交付状态，不是代码部署。
- `W6 30` 可理解为 `font-weight: 600`、`font-size: 30px`；但实际字体是否提供相应字重，仍需在浏览器中确认。
- Figma 中的“余白”需拆开看：容器内部是 Padding，Auto Layout 项目间通常对应 CSS `gap`，普通布局中的间距才可能由 `margin` 实现。
- `span` 中的符号或图标不能只看 `font-size`：还要看 `line-height`、盒子 W/H、显示类型和父级布局。若 Figma 明确给了固定 W/H，使用 `inline-flex` / `inline-block` 让盒子尺寸可控。
- 浏览器 Computed 面板显示的是最终参与渲染的值；当数值不符合预期时，应展开属性来源，检查 CSS 优先级、继承、`flex-shrink`、`box-sizing`、`max-width` 等因素。

对应资料：[2026-07-14 复盘日报](./2026-07-14_复盘日报.md)、[20260715](./20260715)、[README 原有字重说明的来源内容](./20260715)。

### 6. 记录与协作方式

- Markdown 适合日常复盘与技术笔记：可 Git 管理、可查看 diff、可在 VS Code / GitHub 直接预览。
- 图片应使用相对路径并放在有意义的 `assets/` 子目录，不使用本机绝对路径或 Base64。
- Google Colab 可作为临时 Python / 数据处理环境；应以 `/content` 为主要工作目录，避免改动系统级目录。

## Colab + Playwright：FAQ Site 自动化 IT 测试

已补充可执行攻略：[Experience Cloud FAQ Site 自动化 IT 测试攻略](./Experience_Cloud_FAQ_Automated_Testing_Guide.md)。建议先用 Sandbox 的 5 条高频 FAQ 做只读 POC：首页可访问、关键字搜索、Category 层级导航、文章详情和非公开文章的访客不可见。

分工上，Colab 用于维护 `faq_cases.csv`、生成/汇总测试数据；Playwright 用真实浏览器完成 Guest User 路径和断言，并在失败时保留截图、Trace 与 HTML Report。POC 稳定后，把脚本放入 GitHub Actions 或专用 Runner 做提交后/每日运行；不要把临时的 Colab 会话当作长期 CI。

关键边界：测试必须使用未登录的新浏览器上下文，且不在 Notebook、CSV 或 Git 中保存 Org 密码、Session 或令牌。这样能够把“页面找不到内容”“Guest User 权限不足”和“UI 改版造成选择器失效”分别定位。

## 当前待验证事项

| 优先级 | 待验证事项 | 为什么重要 |
|---|---|---|
| 高 | 实际 Guest User 能否仅访问应公开的 Knowledge 与字段 | 这是站点对外开放的安全边界 |
| 高 | 官网与 Experience Cloud 的 CSP / Clickjack 配置 | 直接决定 iframe 方案是否可用 |
| 高 | Category API Name 是否被 LWC、Apex、导入脚本或既有页面引用 | 避免改分类元数据后破坏现有逻辑 |
| 中 | Google Search Console 的负责人、站点验证与 SEO 指标采集 | 没有归属就无法验证独立文章 URL 的 SEO 效果 |
| 中 | 第三方 Cookie 对 iframe 内登录态或交互的影响 | Demo 正常不代表生产浏览器环境正常 |
| 中 | Figma → Gemini → LWC 是否是团队真实工作流及其代码审查方式 | 不能把“听说的流程”当作既定规范 |
| 低 | 设计稿中固定图标 W/H、全局 CSS 覆盖与组件局部样式策略 | 影响 UI 还原效率与后期维护性 |

## 推荐的下一步顺序

1. 在 Sandbox 用少量 FAQ 跑通 Knowledge → Category → Experience Cloud → Guest User 的端到端验证。
2. 用匿名窗口验证独立 Knowledge URL、字段可见性和 iframe 嵌入；记录响应头和浏览器控制台结果。
3. 固化 Category / Field Mapping，并对未匹配项建立人工 Review 清单。
4. 确认管理端 List View、Record Page 与 Custom Report Type 是否满足批量维护与报表筛选需要。
5. 将每天的新结论写入对应专题文件；把“已验证结果”和“待确认问题”分区维护。

## 文件定位

| 类型 | 主要文件 |
|---|---|
| 日常复盘 | `2026-07-13_入职第一天复盘.md`、`2026-07-14_复盘日报.md` |
| 迁移执行 | `runBook.md`、`0722`、`Salesforce 富文本图片迁移标准作业流程` |
| Experience Cloud | `Experience_Cloud_速查清单.md`、`iframe嵌套ExperienceCloud.md`、`0723 iframe关联确认` |
| 分类与报表 | `Salesforce Data Category Group XML の全体構造まとめ.md`、`FAQ_Knowledge_with_Categories.reportType-meta.xml`、`listview確認` |
| Figma / CSS | `20260715`、`2026-07-14_复盘日报.md` |
| 工具与环境 | `0723 _Google Colaboratory`、图片资料 |

---

最后更新：2026-08-09。本文是对现有记录的整理，不替代项目设计书、生产权限审计或变更评审。
